function animationTo(element, keyframes, options) {
    const animated = element.animate(keyframes, { ...options, fill: "both" });
    animated.addEventListener("finish", () => {
        animated.commitStyles();
        animated.cancel();
    });
    return animated;
}
const keyframeDefaults = {
    easing: "cubic-bezier(0.390, 0.575, 0.565, 1.000)",
};
const KEYFRAMES = {
    fadeIn: [
        {
            offset: 0,
            ...keyframeDefaults,
            opacity: 0,
        },
        {
            offset: 1,
            ...keyframeDefaults,
            opacity: 1,
        },
    ],
    fadeOut: [
        {
            offset: 0,
            ...keyframeDefaults,
            opacity: 1,
        },
        {
            offset: 1,
            ...keyframeDefaults,
            opacity: 0,
        },
    ],
};

export { KEYFRAMES as K, animationTo as a };
