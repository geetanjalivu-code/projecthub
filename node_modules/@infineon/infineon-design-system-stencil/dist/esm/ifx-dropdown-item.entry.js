import { r as registerInstance, c as createEvent, a as getElement, h } from './index-Cc7fFwRM.js';

const dropdownItemCss = () => `.dropdown-item{text-decoration:none;color:#1d1d1d;display:flex;align-items:center;padding:8px 16px;gap:8px;font-family:var(--ifx-font-family)}.dropdown-item:focus{outline:none;box-shadow:0 0 0 2px #ffffff, 0 0 0 4px #0a8276}.dropdown-item.hide{display:none}.dropdown-item span{color:#1d1d1d;font-size:16px;font-style:normal;font-weight:400;line-height:24px;}.dropdown-item.small span{font-size:14px}.dropdown-item:hover{cursor:pointer;background-color:#eeeded}.dropdown-item:active{background-color:#bfbbbb}.dropdown-item.error{color:#cd002f}.dropdown-item.error span{color:#cd002f}.dropdown-item.disabled{color:#bfbbbb;pointer-events:none}.dropdown-item.disabled span{color:#bfbbbb}.icon{margin-right:4px}button.dropdown-item{width:100%;border:0;background-color:transparent;text-align:left}`;

const DropdownItem = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.ifxDropdownItem = createEvent(this, "ifxDropdownItem", 7);
    }
    /** Icon to show in the dropdown item. */
    icon = "";
    /** Link the dropdown item should navigate to when clicked. */
    href = "";
    /** Where to open the link. */
    target = "_self";
    /** If true, this dropdown item is hidden. */
    hide = false;
    /** If true, this item is shown in an error style. */
    error = false;
    /** If true, this item is disabled and not interactive. */
    disabled = false;
    size = "l";
    /** Fired when the dropdown item is clicked or selected. */
    ifxDropdownItem;
    get el() { return getElement(this); }
    handleMenuSize(event) {
        this.size = event.detail;
    }
    handleHostClick(e) {
        if (this.disabled) {
            e.stopPropagation();
        }
    }
    handleEventEmission(e) {
        if (this.disabled) {
            e.preventDefault();
            e.stopPropagation();
            return;
        }
        this.ifxDropdownItem.emit(this.el.textContent);
    }
    render() {
        const hasHref = this.href !== undefined && this.href !== null && this.href !== "";
        const common = {
            class: `dropdown-item ${this.size === "s" ? "small" : ""} ${this.hide ? "hide" : ""} ${this.disabled ? "disabled" : this.error ? "error" : ""}`,
            onClick: (e) => this.handleEventEmission(e),
            role: "menuitem",
            "aria-disabled": this.disabled ? "true" : undefined,
        };
        if (this.disabled) {
            common.tabIndex = -1;
        }
        else if (!hasHref) {
            common.tabIndex = 0;
        }
        const content = [
            this.icon && h("ifx-icon", { key: 'd7b967e5900d929cee06ac3a89828bc6e5efe073', class: "icon", icon: this.icon }),
            h("span", { key: '16084374bcb36a4c8547a02398fd48ceba1b8a38' }, h("slot", { key: '285646d921ae3799cb318b8e7daad3a68f15e7f0' })),
        ];
        return hasHref ? (h("a", { ...common, href: this.href, target: this.target }, content)) : (h("button", { ...common, type: "button", disabled: this.disabled }, content));
    }
};
DropdownItem.style = dropdownItemCss();

export { DropdownItem as ifx_dropdown_item };
