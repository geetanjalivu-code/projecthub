import { h } from "@stencil/core";
export class CardHeadline {
    el;
    direction;
    hasDesc;
    componentWillLoad() {
        const cardElement = this.el.closest("ifx-card");
        if (cardElement) {
            const cardClass = cardElement.shadowRoot.querySelector(".card")?.className;
            if (cardClass && cardClass.includes("horizontal")) {
                this.direction = "horizontal";
            }
            const desc = cardElement.querySelector("ifx-card-text");
            if (desc) {
                this.hasDesc = true;
            }
        }
    }
    render() {
        return (h("div", { key: 'e5215de071a90b2ab02b16778f2d44790788a987', class: `card__headline-wrapper ${this.hasDesc ? "withDesc" : ""}` }, h("div", { key: '16bc0e82677b301ea459f39310acaa8b270821a6', class: `card-headline ${this.direction}` }, h("slot", { key: '8ca8ef57b7360478f8865cbeceff27147801ac3b' }))));
    }
    static get is() { return "ifx-card-headline"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() {
        return {
            "$": ["card-headline.scss"]
        };
    }
    static get styleUrls() {
        return {
            "$": ["card-headline.css"]
        };
    }
    static get states() {
        return {
            "direction": {},
            "hasDesc": {}
        };
    }
    static get elementRef() { return "el"; }
}
