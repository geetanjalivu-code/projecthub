import { Host, h, } from "@stencil/core";
import { isNestedInIfxComponent } from "../../../shared/utils/dom-utils";
import { detectFramework } from "../../../shared/utils/framework-detection";
import { trackComponent } from "../../../shared/utils/tracking";
import { applyToastConfig } from "../toast-config";
export class ToastContainer {
    el;
    /** Placement of the container on desktop. Collapses to top/bottom on mobile. */
    placement = "bottom-right";
    /** Distance in px from the viewport edge. Added on top of the navbar clearance for top placements. */
    offset = 16;
    /** CSS selector of the navbar/header to keep clear of on top placements. Empty disables measuring. */
    navbarSelector = "ifx-navbar";
    /** Maximum number of simultaneously visible toasts. `0` means unlimited. */
    max = 0;
    navbarClearance = 0;
    resizeRaf;
    async componentDidLoad() {
        if (!isNestedInIfxComponent(this.el)) {
            const framework = detectFramework();
            trackComponent("ifx-toast-container", await framework);
        }
        this.updateNavbarClearance();
        if (typeof window !== "undefined") {
            window.addEventListener("resize", this.handleResize);
        }
    }
    disconnectedCallback() {
        if (typeof window !== "undefined") {
            window.removeEventListener("resize", this.handleResize);
            if (this.resizeRaf) {
                cancelAnimationFrame(this.resizeRaf);
            }
        }
    }
    /**
     * Creates an `ifx-toast`, appends it to the container, and removes it once dismissed.
     * Returns the created element so callers can update or dismiss it.
     */
    async addToast(config = {}) {
        this.updateNavbarClearance();
        const toast = document.createElement("ifx-toast");
        applyToastConfig(toast, config);
        toast.addEventListener("ifxToastClose", () => toast.remove(), { once: true });
        this.el.appendChild(toast);
        this.enforceMax();
        return toast;
    }
    /** Dismisses every toast currently in the container. */
    async dismissAll() {
        await Promise.all(this.getToasts().map((toast) => toast.dismiss?.()));
    }
    /**
     * Dismisses the oldest toasts until at most `max` remain (`max <= 0` disables the
     * limit). Public so the `ifxToast` controller can enforce the cap after appending a
     * toast directly, not only via `addToast`.
     */
    async enforceMax() {
        if (this.max <= 0) {
            return;
        }
        const toasts = this.getToasts();
        const excess = toasts.length - this.max;
        // Oldest toasts come first in DOM order; dismiss them until within the limit.
        for (let i = 0; i < excess; i++) {
            toasts[i].dismiss?.();
        }
    }
    getToasts() {
        return Array.from(this.el.querySelectorAll("ifx-toast"));
    }
    measureNavbar() {
        if (typeof document === "undefined") {
            return 0;
        }
        const selector = this.navbarSelector?.trim();
        if (!selector) {
            return 0;
        }
        const navbar = document.querySelector(selector);
        if (!navbar) {
            return 0;
        }
        return Math.max(0, navbar.getBoundingClientRect().bottom);
    }
    updateNavbarClearance = () => {
        this.navbarClearance = this.measureNavbar();
    };
    handleResize = () => {
        if (typeof window === "undefined") {
            return;
        }
        if (this.resizeRaf) {
            cancelAnimationFrame(this.resizeRaf);
        }
        this.resizeRaf = requestAnimationFrame(this.updateNavbarClearance);
    };
    render() {
        return (h(Host, { key: '72ba0073447f8394468a11f035c2649e81494b1a', style: {
                "--ifx-toast-offset": `${this.offset}px`,
                "--ifx-toast-navbar": `${this.navbarClearance}px`,
            } }, h("section", { key: '1c79cd59257b5fa4adbebeeba145cafeae38b8b3', class: `ifx-toast-container ifx-toast-container--${this.placement}`, "aria-label": "Notifications" }, h("slot", { key: 'b8f70ffe3b0711f7f4134c139059c9bad1521dc4' }))));
    }
    static get is() { return "ifx-toast-container"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() {
        return {
            "$": ["toast-container.scss"]
        };
    }
    static get styleUrls() {
        return {
            "$": ["toast-container.css"]
        };
    }
    static get properties() {
        return {
            "placement": {
                "type": "string",
                "mutable": false,
                "complexType": {
                    "original": "ToastPlacement",
                    "resolved": "\"bottom-center\" | \"bottom-left\" | \"bottom-right\" | \"top-center\" | \"top-left\" | \"top-right\"",
                    "references": {
                        "ToastPlacement": {
                            "location": "local",
                            "path": "/home/runner/work/infineon-design-system-stencil/infineon-design-system-stencil/packages/components/src/components/toast/toast-container/toast-container.tsx",
                            "id": "src/components/toast/toast-container/toast-container.tsx::ToastPlacement"
                        }
                    }
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Placement of the container on desktop. Collapses to top/bottom on mobile."
                },
                "getter": false,
                "setter": false,
                "reflect": true,
                "attribute": "placement",
                "defaultValue": "\"bottom-right\""
            },
            "offset": {
                "type": "number",
                "mutable": false,
                "complexType": {
                    "original": "number",
                    "resolved": "number",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Distance in px from the viewport edge. Added on top of the navbar clearance for top placements."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "attribute": "offset",
                "defaultValue": "16"
            },
            "navbarSelector": {
                "type": "string",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "CSS selector of the navbar/header to keep clear of on top placements. Empty disables measuring."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "attribute": "navbar-selector",
                "defaultValue": "\"ifx-navbar\""
            },
            "max": {
                "type": "number",
                "mutable": false,
                "complexType": {
                    "original": "number",
                    "resolved": "number",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Maximum number of simultaneously visible toasts. `0` means unlimited."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "attribute": "max",
                "defaultValue": "0"
            }
        };
    }
    static get states() {
        return {
            "navbarClearance": {}
        };
    }
    static get methods() {
        return {
            "addToast": {
                "complexType": {
                    "signature": "(config?: ToastConfig) => Promise<HTMLIfxToastElement>",
                    "parameters": [{
                            "name": "config",
                            "type": "ToastConfig",
                            "docs": ""
                        }],
                    "references": {
                        "Promise": {
                            "location": "global",
                            "id": "global::Promise"
                        },
                        "HTMLIfxToastElement": {
                            "location": "global",
                            "id": "global::HTMLIfxToastElement"
                        },
                        "ToastConfig": {
                            "location": "local",
                            "path": "/home/runner/work/infineon-design-system-stencil/infineon-design-system-stencil/packages/components/src/components/toast/toast-container/toast-container.tsx",
                            "id": "src/components/toast/toast-container/toast-container.tsx::ToastConfig"
                        }
                    },
                    "return": "Promise<HTMLIfxToastElement>"
                },
                "docs": {
                    "text": "Creates an `ifx-toast`, appends it to the container, and removes it once dismissed.\nReturns the created element so callers can update or dismiss it.",
                    "tags": []
                }
            },
            "dismissAll": {
                "complexType": {
                    "signature": "() => Promise<void>",
                    "parameters": [],
                    "references": {
                        "Promise": {
                            "location": "global",
                            "id": "global::Promise"
                        }
                    },
                    "return": "Promise<void>"
                },
                "docs": {
                    "text": "Dismisses every toast currently in the container.",
                    "tags": []
                }
            },
            "enforceMax": {
                "complexType": {
                    "signature": "() => Promise<void>",
                    "parameters": [],
                    "references": {
                        "Promise": {
                            "location": "global",
                            "id": "global::Promise"
                        }
                    },
                    "return": "Promise<void>"
                },
                "docs": {
                    "text": "Dismisses the oldest toasts until at most `max` remain (`max <= 0` disables the\nlimit). Public so the `ifxToast` controller can enforce the cap after appending a\ntoast directly, not only via `addToast`.",
                    "tags": []
                }
            }
        };
    }
    static get elementRef() { return "el"; }
}
