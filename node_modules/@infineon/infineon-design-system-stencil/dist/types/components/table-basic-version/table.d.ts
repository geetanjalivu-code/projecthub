import { type GridOptions } from "ag-grid-community";
export declare class Table {
    gridOptions: GridOptions;
    /** Column definitions for the table (array or JSON string). */
    readonly cols: any[] | string;
    /** Row data for the table (array or JSON string). */
    readonly rows: any[] | string;
    columnDefs: any[];
    rowData: any[];
    /** Row height preset (e.g. default, compact). */
    readonly rowHeight: string;
    /** Overall table height (e.g. fixed px value or 'auto'). */
    readonly tableHeight: string;
    /** Visual style variant of the table (e.g. default, compact). */
    readonly variant: string;
    uniqueKey: string;
    host: HTMLIfxBasicTableElement;
    private container;
    private gridApi;
    private gridInitialized;
    componentWillLoad(): void;
    private setGridOptions;
    private setColsAndRows;
    private getRowData;
    private getColData;
    private onFirstDataRendered;
    componentWillUpdate(): void;
    componentDidLoad(): Promise<void>;
    private getClassNames;
    private getTableStyle;
    render(): any;
}
