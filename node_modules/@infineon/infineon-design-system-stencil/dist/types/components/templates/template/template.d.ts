import { type EventEmitter } from "../../../stencil-public-runtime";
export declare class Template {
    el: HTMLIfxTemplateElement;
    repoDetails: {
        name: string;
        desc: string;
        framework: string;
    };
    repoUrl: string;
    showDetails: boolean;
    isTemplatePage: boolean;
    isLoading: boolean;
    repoError: string;
    /** Name of the template field. */
    readonly name: string;
    /** Fired when templates are toggled on or off. */
    toggleTemplates: EventEmitter;
    /** Fired when a validation error occurs on the field. */
    fieldError: EventEmitter;
    /** Thumbnail image URL or identifier for the template. */
    readonly thumbnail: string;
    private clientId;
    private redirectUri;
    private scope;
    private state;
    private authUser;
    componentDidLoad(): void;
    private getLocalStorageValues;
    private getUserToken;
    private handleUserInput;
    private submitUserData;
    private togglePadding;
    private handleCurrentTemplate;
    /** Toggles visibility of this template and hides the currently open one if needed. */
    toggleTemplate(currTemp: any): Promise<void>;
    render(): any;
}
