export declare class Tooltip {
    el: HTMLIfxTooltipElement;
    tooltipVisible: boolean;
    /** Header text displayed at the top of the tooltip. */
    readonly header: string;
    /** Main content text displayed inside the tooltip. */
    readonly text: string;
    /** Preferred position of the tooltip relative to the trigger element. */
    readonly position: "bottom-start" | "top-start" | "left" | "bottom-end" | "top-end" | "right" | "bottom" | "top" | "auto";
    internalPosition: "bottom-start" | "top-start" | "left" | "bottom-end" | "top-end" | "right" | "bottom" | "top" | "auto";
    /** Accessible label for screen readers. */
    readonly ariaLabelText: string | null;
    /** Visual style variant of the tooltip. */
    readonly variant: "compact" | "dismissible" | "extended";
    /** Optional icon displayed in the tooltip. */
    readonly icon: string;
    /** If true, appends the tooltip element to document.body for positioning. */
    readonly appendToBody: boolean;
    private tooltipContainer;
    private tooltipEl;
    private referenceEl;
    private popperInstance;
    private getVariant;
    componentDidLoad(): Promise<void>;
    private initializePopper;
    private determineBestPosition;
    positionChanged(newVal: any): void;
    private onMouseEnter;
    private onMouseLeave;
    disconnectedCallback(): void;
    private onClick;
    private onDismissClick;
    render(): any;
}
