'use strict';

var index = require('./index-OWCBTetj.js');

const dropdownTriggerCss = () => `:host{display:inline-block;cursor:pointer}`;

const DropdownItem = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
    }
    /** Controls wether the dropdown is currently open. */
    isOpen = false;
    render() {
        return index.h("slot", { key: 'c16509f761f38f9c51f060564df372cec2f4f873' });
    }
};
DropdownItem.style = dropdownTriggerCss();

exports.ifx_dropdown_trigger = DropdownItem;
