'use strict';

var index = require('./index-OWCBTetj.js');
var domUtils = require('./dom-utils-BdvOgC2b.js');
var tracking = require('./tracking-BPjaTlNR.js');

const sliderCss = () => `:host{display:block}.left-icon,.right-icon,.left-text,.right-text,.percentage-display{font-family:var(--ifx-font-family);font-size:0.875rem;color:#575352;margin-left:8px}.left-icon.disabled,.right-icon.disabled,.left-text.disabled,.right-text.disabled,.percentage-display.disabled{color:#bfbbbb}.left-icon,.left-text{margin-right:8px}.right-icon,.right-text,.percentage-display{margin-left:8px}.ifx-slider{display:flex;align-items:center;padding:2px 16px;border-radius:9999px}.ifx-slider input[type=range]{-webkit-appearance:none;appearance:none;width:100%;height:4px;background:linear-gradient(to right, #0a8276 0%, #0a8276 var(--value-percent, 0%), #eeeded var(--value-percent, 0%), #eeeded 100%);outline:none;cursor:pointer;transition:0.2s}.ifx-slider input[type=range]:focus-visible{outline:2px solid #0a8276;outline-offset:4px}.ifx-slider input[type=range]::-moz-range-thumb{width:20px;height:20px;border-radius:50%;background:#0a8276;cursor:pointer;box-shadow:0px 1px 2px rgba(0, 0, 0, 0.2)}.ifx-slider input[type=range]:not(:disabled)::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;width:16px;height:16px;background:#0a8276;border-radius:50%;cursor:pointer;transition:box-shadow 0.2s}.ifx-slider input[type=range]:not(:disabled)::-webkit-slider-thumb:hover{background:#08665c}.ifx-slider input[type=range]:not(:disabled)::-webkit-slider-thumb:active{background:#06534b}.ifx-slider input[type=range]:disabled{background:#bfbbbb;cursor:default}.ifx-slider input[type=range]:disabled::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;width:16px;height:16px;background:#bfbbbb;border-radius:50%;cursor:default}.ifx-slider.readOnly input[type=range]{pointer-events:none;background:linear-gradient(to right, #575352 50%, #bfbbbb var(--value-percent, 0%), #eeeded var(--value-percent, 0%), #eeeded 100%)}.ifx-slider.readOnly input[type=range]::-webkit-slider-thumb{background:#bfbbbb}.ifx-slider .range-slider__wrapper{position:relative;width:100%;height:4px;display:flex;align-items:center;justify-content:center;margin:0px 2px;background:linear-gradient(to right, #eeeded 0%, #eeeded var(--min-value-percent, 100%), #0a8276 var(--min-value-percent, 100%), #0a8276 var(--max-value-percent, 100%), #eeeded var(--max-value-percent, 100%), #eeeded 100%)}.ifx-slider .range-slider__wrapper:has(input[type=range]:disabled){background:#bfbbbb}.ifx-slider .range-slider__wrapper input[type=range]{position:absolute;pointer-events:none;background:none}.ifx-slider .range-slider__wrapper input[type=range]:focus-visible{outline:2px solid #0a8276;outline-offset:4px}.ifx-slider .range-slider__wrapper input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;border:none;pointer-events:auto;cursor:pointer;box-shadow:0px 1px 2px rgba(0, 0, 0, 0.2)}.ifx-slider .range-slider__wrapper input[type=range]::-moz-range-thumb{-webkit-appearance:none;appearance:none;border:none;pointer-events:auto;cursor:pointer;box-shadow:0px 1px 2px rgba(0, 0, 0, 0.2)}.ifx-slider .range-slider__wrapper input[type=range]::-ms-thumb{-webkit-appearance:none;appearance:none;border:none;pointer-events:auto;cursor:pointer;box-shadow:0px 1px 2px rgba(0, 0, 0, 0.2)}.ifx-slider .range-slider__wrapper input[type=range]::-webkit-slider-runnable-track{-webkit-appearance:none;appearance:none;border:none;background:none;width:100%}.ifx-slider .range-slider__wrapper input[type=range]::-moz-range-track{-webkit-appearance:none;appearance:none;border:none;background:none;width:100%}`;

const IfxSlider = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.ifxChange = index.createEvent(this, "ifxChange", 7);
        if (hostRef.$hostElement$["s-ei"]) {
            this.internals = hostRef.$hostElement$["s-ei"];
        }
        else {
            this.internals = hostRef.$hostElement$.attachInternals();
            hostRef.$hostElement$["s-ei"] = this.internals;
        }
    }
    /** Minimum value allowed for the slider. */
    min = 0;
    /** Maximum value allowed for the slider. */
    max = 100;
    /** Increment step when changing the value. */
    step = 1;
    /** Current value for a single-handle slider. */
    value;
    /** Initial value of the left handle for a double slider. */
    minValueHandle;
    /** Initial value of the right handle for a double slider. */
    maxValueHandle;
    /** If true, the slider is disabled and not interactive. */
    disabled = false;
    /** If true, the slider is read-only. */
    readOnly = false;
    /** If true, shows the current value as a percentage. */
    showPercentage = false;
    /** Optional icon displayed on the left side of the slider. */
    leftIcon;
    /** Optional icon displayed on the right side of the slider. */
    rightIcon;
    /** Optional text label shown on the left side of the slider. */
    leftText;
    /** Optional text label shown on the right side of the slider. */
    rightText;
    /** Slider type: single handle or double handle (range). */
    type = "single";
    /** Accessible label for screen readers. */
    ariaLabelText;
    /** Optional name used when submitting the slider in a form. */
    name = "";
    internalValue = 0;
    percentage = 0;
    internalMinValue = 0;
    internalMaxValue = 100;
    /** Fired when the slider value (or values) change. */
    ifxChange;
    get el() { return index.getElement(this); }
    internals;
    inputRef;
    minInputRef;
    maxInputRef;
    hasInitializedSingleValue = false;
    initialInternalValue = 0;
    initialInternalMinValue = 0;
    initialInternalMaxValue = 0;
    getSafeMin() {
        const parsedMin = Number(this.min);
        return Number.isFinite(parsedMin) ? parsedMin : 0;
    }
    getSafeMax() {
        const parsedMax = Number(this.max);
        return Number.isFinite(parsedMax) ? parsedMax : this.getSafeMin();
    }
    normalizeSingleValue(value) {
        const numericValue = Number(value);
        const safeMin = this.getSafeMin();
        const safeMax = this.getSafeMax();
        if (!Number.isFinite(numericValue)) {
            return safeMin;
        }
        return Math.max(safeMin, Math.min(safeMax, numericValue));
    }
    getSynchronizedSingleValue() {
        const hasExternalValue = Number.isFinite(Number(this.value));
        const sourceValue = hasExternalValue ? this.value : this.internalValue;
        return this.normalizeSingleValue(sourceValue);
    }
    valueChanged(newValue) {
        this.internalValue = this.normalizeSingleValue(newValue);
        this.value = this.internalValue;
        this.calculatePercentageValue();
        this.updateValuePercent();
        this.updateFormValue();
    }
    minValueChanged(newValue) {
        this.internalMinValue = newValue;
        this.updateValuePercent();
        this.updateFormValue();
    }
    maxValueChanged(newValue) {
        this.internalMaxValue = newValue;
        this.updateValuePercent();
        this.updateFormValue();
    }
    minChanged() {
        this.reconcileBoundsChange();
    }
    maxChanged() {
        this.reconcileBoundsChange();
    }
    reconcileBoundsChange() {
        if (this.type !== "double" && !this.hasInitializedSingleValue) {
            return;
        }
        this.internalValue = this.getSynchronizedSingleValue();
        this.value = this.internalValue;
        this.calculatePercentageValue();
        this.updateValuePercent();
        this.updateFormValue();
    }
    stepChanged() {
        this.updateValuePercent();
    }
    updateFormValue() {
        if (this.type === "double") {
            this.internals.setFormValue(JSON.stringify({
                min: this.internalMinValue,
                max: this.internalMaxValue,
            }));
            return;
        }
        this.internals.setFormValue(String(this.internalValue));
    }
    getRangeSliderWrapper() {
        const rangeSliderWrapper = this.el.shadowRoot.querySelector(".range-slider__wrapper");
        return rangeSliderWrapper;
    }
    handleInputChangeOfRangeSlider(event) {
        const target = event.target;
        if (parseFloat(this.maxInputRef.value) - parseFloat(this.minInputRef.value) <=
            0) {
            if (target.id === "max-slider") {
                this.maxInputRef.value = this.minInputRef.value;
            }
            else {
                this.minInputRef.value = this.maxInputRef.value;
            }
        }
        if (target.id === "max-slider") {
            this.internalMaxValue = parseFloat(this.maxInputRef.value);
        }
        else {
            this.internalMinValue = parseFloat(this.minInputRef.value);
        }
        this.ifxChange.emit({
            minVal: this.internalMinValue,
            maxVal: this.internalMaxValue,
        });
        this.updateFormValue();
        this.updateValuePercent();
        this.updateZIndexIfRangeSlider(target.id);
    }
    handleOnMouseLeaveOfRangeSlider(event) {
        const target = event.target;
        const sliderWrapper = this.getRangeSliderWrapper();
        if (target.id === "max-slider") {
            sliderWrapper.insertBefore(this.maxInputRef, this.minInputRef);
        }
        else {
            sliderWrapper.insertBefore(this.minInputRef, this.maxInputRef);
        }
    }
    calculatePercentageValue() {
        const parsedInternalValue = Number(this.internalValue);
        const safeMin = this.getSafeMin();
        const safeMax = this.getSafeMax();
        const den = safeMax - safeMin;
        if (den <= 0 || !Number.isFinite(parsedInternalValue)) {
            this.percentage = 0;
            return;
        }
        const num = (parsedInternalValue - safeMin) * 1.0;
        this.percentage = +parseFloat(String((num / den) * 100)).toFixed(2);
    }
    handleInputChange(event) {
        const target = event.target;
        this.internalValue = this.normalizeSingleValue(target.value);
        this.value = this.internalValue;
        this.ifxChange.emit(this.internalValue);
        this.updateFormValue();
        this.calculatePercentageValue();
        this.updateValuePercent();
    }
    roundToValidStep(value) {
        const safeMin = this.getSafeMin();
        const safeStep = Number.isFinite(Number(this.step)) && Number(this.step) > 0 ? Number(this.step) : 1;
        const relativeValue = value - safeMin;
        const remainder = relativeValue % safeStep;
        if (remainder >= safeStep / 2) {
            return safeMin + relativeValue + (safeStep - remainder);
        }
        else {
            return safeMin + relativeValue - remainder;
        }
    }
    updateValuePercent() {
        const safeMin = this.getSafeMin();
        const safeMax = this.getSafeMax();
        const den = safeMax - safeMin;
        if (den <= 0) {
            if (this.inputRef) {
                this.inputRef.style.setProperty("--value-percent", "0%");
            }
            return;
        }
        if (this.type === "double") {
            if (this.minInputRef) {
                const num = (this.roundToValidStep(this.internalMinValue) - safeMin) * 1.0;
                const minPercent = (num / den) * 100;
                this.minInputRef.parentElement.style.setProperty("--min-value-percent", `${minPercent}%`);
            }
            if (this.maxInputRef) {
                const num = (this.roundToValidStep(this.internalMaxValue) - safeMin) * 1.0;
                const maxPercent = (num / den) * 100;
                this.maxInputRef.parentElement.style.setProperty("--max-value-percent", `${maxPercent}%`);
            }
        }
        else {
            if (this.inputRef) {
                const num = (this.roundToValidStep(this.internalValue) - safeMin) * 1.0;
                const percentage = (num / den) * 100;
                this.inputRef.style.setProperty("--value-percent", `${percentage}%`);
            }
        }
    }
    // Ensures that the last used slider thumb stays on top of the other thumb in order to handle correct overlapping
    // if min and max thumbs take the same value.
    updateZIndexIfRangeSlider(targetId = "") {
        if (targetId === "max-slider") {
            this.minInputRef.style.zIndex = "1";
            this.maxInputRef.style.zIndex = "2";
        }
        else {
            this.minInputRef.style.zIndex = "2";
            this.maxInputRef.style.zIndex = "1";
        }
    }
    componentWillLoad() {
        const safeMin = this.getSafeMin();
        const safeMax = this.getSafeMax();
        const midPoint = (safeMax - safeMin) / 2 + safeMin;
        if (this.value === undefined || this.value === null) {
            this.internalValue = this.normalizeSingleValue(midPoint);
            this.value = this.internalValue;
        }
        else {
            this.internalValue = this.normalizeSingleValue(this.value);
            this.value = this.internalValue;
        }
        this.calculatePercentageValue();
        if (this.minValueHandle !== undefined)
            this.internalMinValue = this.minValueHandle;
        else
            this.internalMinValue = this.min;
        if (this.maxValueHandle !== undefined)
            this.internalMaxValue = this.maxValueHandle;
        else
            this.internalMaxValue = this.max;
        this.initialInternalValue = this.internalValue;
        this.initialInternalMinValue = this.internalMinValue;
        this.initialInternalMaxValue = this.internalMaxValue;
        this.hasInitializedSingleValue = true;
        this.updateFormValue();
    }
    formResetCallback() {
        this.internalValue = this.initialInternalValue;
        this.internalMinValue = this.initialInternalMinValue;
        this.internalMaxValue = this.initialInternalMaxValue;
        this.calculatePercentageValue();
        this.updateValuePercent();
        this.updateFormValue();
    }
    formStateRestoreCallback(state, _mode) {
        if (!state) {
            return;
        }
        if (this.type === "double") {
            try {
                const parsedState = JSON.parse(state);
                if (typeof parsedState.min === "number") {
                    this.internalMinValue = parsedState.min;
                }
                if (typeof parsedState.max === "number") {
                    this.internalMaxValue = parsedState.max;
                }
            }
            catch {
                // Ignore invalid state payloads restored by the browser.
            }
        }
        else {
            const parsedState = Number(state);
            if (!Number.isNaN(parsedState)) {
                this.internalValue = parsedState;
                this.calculatePercentageValue();
            }
        }
        this.updateValuePercent();
        this.updateFormValue();
    }
    syncSingleValueState() {
        if (this.type === "double") {
            return false;
        }
        const normalizedValue = this.getSynchronizedSingleValue();
        if (normalizedValue === this.internalValue) {
            return false;
        }
        this.internalValue = normalizedValue;
        this.value = normalizedValue;
        this.calculatePercentageValue();
        this.updateFormValue();
        return true;
    }
    async componentDidLoad() {
        if (!domUtils.isNestedInIfxComponent(this.el)) {
            const framework = tracking.detectFramework();
            tracking.trackComponent("ifx-slider", await framework);
        }
        // Angular/ngModel can set `value` before custom-element upgrade.
        // Re-sync once after load so internal state and displayed percentage match.
        this.syncSingleValueState();
        this.updateValuePercent();
    }
    render() {
        return (index.h("div", { key: '05c3508673911f163b8ec8cb69589db272c865ff', class: `ifx-slider${this.readOnly ? ' readOnly' : this.disabled ? ' disabled' : ''}` }, this.leftText && index.h("span", { key: '554681b5fcf8d807f680a421add26ffb0c7080ae', class: `left-text${this.readOnly || this.disabled ? ' disabled' : ''}` }, this.leftText), this.leftIcon && (index.h("ifx-icon", { key: '950209e849c2f98b0ed9a7fff10cc9c96fd924bd', icon: this.leftIcon, class: `left-icon${this.readOnly || this.disabled ? " disabled" : ""}` })), this.type !== "double" ? (index.h("input", { type: "range", min: this.min, max: this.max, step: this.step, value: this.internalValue, disabled: this.readOnly || this.disabled, ref: (el) => (this.inputRef = el), onInput: (event) => this.handleInputChange(event), "aria-label": "Slider", "aria-valuemin": this.min, "aria-valuemax": this.max, "aria-valuenow": this.internalValue, "aria-disabled": this.readOnly || this.disabled ? "true" : "false", "aria-readonly": this.readOnly ? "true" : undefined })) : (index.h("div", { class: "range-slider__wrapper", "aria-label": this.ariaLabelText, role: "group" }, index.h("input", { id: "min-slider", type: "range", min: this.min, max: this.max, step: this.step, value: this.internalMinValue, disabled: this.readOnly || this.disabled, ref: (el) => (this.minInputRef = el), onInput: (event) => this.handleInputChangeOfRangeSlider(event), onMouseUp: (event) => this.handleOnMouseLeaveOfRangeSlider(event), "aria-label": "Minimum value slider", "aria-valuemin": this.min, "aria-valuemax": this.max, "aria-valuenow": this.internalMinValue, "aria-disabled": this.readOnly || this.disabled ? "true" : "false" }), index.h("input", { id: "max-slider", type: "range", min: this.min, max: this.max, step: this.step, value: this.internalMaxValue, disabled: this.readOnly || this.disabled, ref: (el) => (this.maxInputRef = el), onInput: (event) => this.handleInputChangeOfRangeSlider(event), onMouseUp: (event) => this.handleOnMouseLeaveOfRangeSlider(event), "aria-label": "Maximum value slider", "aria-valuemin": this.min, "aria-valuemax": this.max, "aria-valuenow": this.internalMaxValue, "aria-disabled": this.readOnly || this.disabled ? "true" : "false" }))), this.rightIcon && (index.h("ifx-icon", { key: 'fa07a63fb04973a0b9bccbf6a81930ae92e950d3', icon: this.rightIcon, class: `right-icon${this.readOnly || this.disabled ? " disabled" : ""}` })), this.rightText && (index.h("span", { key: 'e89b6ab845c7fc4efe13f2fd77f0e4a3206cf65c', class: `right-text${this.readOnly || this.disabled ? " disabled" : ""}` }, this.rightText)), this.showPercentage && this.type !== "double" && (index.h("span", { key: 'aa680fde1a4ec8fd15d7f3242b7986dbeec90b91', class: `percentage-display${this.readOnly || this.disabled ? " disabled" : ""}` }, this.percentage, "%"))));
    }
    static get formAssociated() { return true; }
    static get watchers() { return {
        "value": [{
                "valueChanged": 0
            }],
        "minValueHandle": [{
                "minValueChanged": 0
            }],
        "maxValueHandle": [{
                "maxValueChanged": 0
            }],
        "min": [{
                "minChanged": 0
            }],
        "max": [{
                "maxChanged": 0
            }],
        "step": [{
                "stepChanged": 0
            }]
    }; }
};
IfxSlider.style = sliderCss();

exports.ifx_slider = IfxSlider;
